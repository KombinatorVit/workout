import React from 'react';
import Layout from "../../layout/Layout.jsx";

const Profile = () => {
    return (
        <Layout>
            Profile
        </Layout>
    );
};

export default Profile;